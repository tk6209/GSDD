name: GSDD Verify

on:
  pull_request:
    paths:
      - "**/*.md"

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run GSDD verify
        run: |
          chmod +x scripts/gsdd
          ./scripts/gsdd verify docs/spec-template.md
